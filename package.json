{
  "name": "doorway",
  "version": "0.1.0",
  "private": true,
  "description": "A verified opportunity intelligence world built and maintained with Bright Data Scraper Studio and the Doorway Trust Engine",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "workspaces": [
    "backend",
    "frontend",
    "driftmart"
  ],
  "scripts": {
    "dev": "npm run build --workspace backend && concurrently --kill-others --names backend-types,backend,frontend,fixture \"npm run dev:types --workspace backend\" \"npm run dev:server --workspace backend\" \"npm run dev --workspace frontend\" \"npm run dev --workspace driftmart\"",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check": "npm run typecheck && npm run lint && npm test",
    "shots": "node scripts/shots.mjs",
    "legibility": "node scripts/legibility.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "phase0": "npm run phase0 --workspace backend",
    "live": "npm run live --workspace backend --",
    "prove": "npm run prove --workspace backend",
    "mcp": "npm run mcp --workspace backend",
    "notice": "npm run notice --workspace backend --",
    "seed": "npm run seed --workspace backend",
    "blindspot": "npm run blindspot --workspace backend --",
    "benchmark": "npm run benchmark --workspace backend",
    "demo:reset": "npm run live -- mode baseline && npm run live -- observe-all",
    "doorway:seed": "npm run build --workspace backend && npm run doorway:seed --workspace backend --",
    "blindspot:proof": "npm run build --workspace backend && npm run blindspot:proof --workspace backend"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@next/eslint-plugin-next": "^15.5.23",
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.10.2",
    "concurrently": "^9.2.4",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.67.0",
    "vitest": "^2.1.8"
  }
}
