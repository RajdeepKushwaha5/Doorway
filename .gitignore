# Doorway project dependencies & workspace node_modules
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo
.next/
out/

# environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Bright Data CLI local credentials
.brightdata/
brightdata-credentials.json

# evidence captures that may contain raw page content
docs/evidence/raw/
*.warc
*.warc.gz

# runtime store: contains scraped page excerpts and incident evidence
data/
backend/data/

# local databases
*.sqlite
*.sqlite-journal
*.db

# logs
logs/
*.log
npm-debug.log*

# editor and OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# test artifacts
coverage/
test-results/
playwright-report/

# planning and rehearsal material, not part of the submission
build-plan.md
notice-demo-script.md

# local visual QA captures (hundreds of MB of screenshots)
.visual-qa/

# Internal working notes: names other projects, not part of the submission.
remaining.md

# Which fault the fixture is currently serving. Runtime state, not source:
# committing it means a fresh deploy starts up serving whatever fault was last
# demonstrated. state.ts already falls back to baseline when the file is absent.
driftmart/.driftmart-mode

RECORDING-SCRIPT.md
