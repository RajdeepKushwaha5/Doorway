export const metadata = {
  title: 'Verified decisions',
  description: 'Compare raw collector recommendations with the NOTICE verified feed.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
