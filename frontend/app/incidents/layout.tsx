export const metadata = {
  title: 'Incident evidence',
  description: 'Inspect the evidence, diagnosis, and repair gate for a NOTICE incident.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
